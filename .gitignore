# ---- Terraform ------------------------------------------------------------
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl is intentionally NOT ignored (best practice: commit
# the lock file so provider versions stay reproducible).

# ---- Secrets / credentials ------------------------------------------------
# Never commit files holding Azure / AWS credentials.
*.tfvars.secret
*.secret
*.secrets
.env
.envrc
credentials
credentials.json

# ---- OS / editor ----------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
